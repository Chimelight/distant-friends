# distant-friends — Claude Code 入口

事实层真相 = 代码与数据；意图层真相 = `DESIGN.md`（维护规则见其 §0）。本文件只是常驻指针层——只指路不复述，与 DESIGN.md 冲突时以 DESIGN.md 为准。

## 红线（详见 DESIGN.md §2，方向性原则不动摇）

1. 美学优先——绝不允许"AI 网页模板"观感（Inter、紫渐变、通用圆角卡片）
2. 内容是主角，UI 消隐
3. 一次加载永久可用（PWA 离线优先）
4. 键盘可达 + 读屏友好，AA 起步
5. 轻而快（预算见 §9，按实测校准）

## 会话纪律

- 功能已冻结（§12 基本全部「不做」）；视觉改动**一轮一验**：先交截图取得用户认可，再开下一轮——禁止无人验收的批量交付（§13 2026-07-07）
- 一个会话做一轮，在 commit 边界收束；跨会话状态由 §15 账本承接
- 提交门禁：`pnpm build` + `pnpm check`（改交互再加 `pnpm test` 冒烟）

## DESIGN.md 阅读地图（按任务选读，不必全文）

- 改内容（短语 / 语言 / 场景）→ §5 + README
- 设计打磨轮 → §15 + §13 末尾数条 + 相关 §6 小节 + §2
- 性能 → §9 · 无障碍 → §8 + §7.2 · 部署 / CI / 依赖 → §10
- 动文档自身（§0 / §13 / §15）→ 先读 §0 维护规则
